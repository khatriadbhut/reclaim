# Optional — you can skip this file entirely.
# Only needed if you want /terms and /privacy "Extension setup" to work when the page
# is opened WITHOUT ?ext= (e.g. landing footer, bookmark). Opening legal links from
# onboarding already passes ?ext= — no env required for that.
# VITE_RECLAIM_EXTENSION_ID=your_32_char_id_from_chrome_extensions
