# Optional: Chrome extension id from chrome://extensions (Developer mode).
# Lets /terms and /privacy "Extension setup" link work when opened without ?ext= (e.g. from landing footer).
# VITE_RECLAIM_EXTENSION_ID=abcdefghijklmnopqrst
